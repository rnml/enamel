(
  (tms (
    (Kind (Variant (
      (Star ())
      (Arr ($Kind $Kind))
    )))
    (Type (Variant (
      (Name   ((Var Type)))
      (Arr    ($Type $Type))
      (Record ((Map Label $Type)))
      (Forall ((Bind $Ty_bnd $Type)))
      (Exists ((Bind $Ty_bnd $Type)))
      (Fun    ((Bind $Ty_bnd $Type)))
      (App    ($Type $Type))
    )))
    (Term (Variant (
      (Name   ((Var Term)))
      (Fun    ((Bind (Tuple ((Var Term) $Type)) $Term)))
      (App    ($Term $Term))
      (Record ((Map Label $Term)))
      (Dot    ($Term $Label))
      (Ty_fun ((Bind $Ty_bnd $Term)))
      (Ty_app ($Term $Type))
      (Pack   ((Bind (Tuple ((Var Type) (Embed $Type))) (Tuple ($Term $Type)))))
      (Unpack ((Bind (Tuple ((Var Type) (Var Term) (Embed $Term))) $Term)))
      (Let    ((Bind (Tuple ((Var Term) (Embed $Term))) $Term)))
    )))
  ))
  (pts (
    (Ty_bnd (Synonym (Tuple ((Var Type) $Kind))))
  ))
)
